apiVersion: v1
kind: Service
metadata:
  name: {{ include "capv-helm-chart.fullname" . }}
  labels:
    {{- include "capv-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capv-helm-chart.selectorLabels" . | nindent 4 }}
